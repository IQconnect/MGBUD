 <header class="@if($class) {{ $class }} @endif">
 <h2 class="section-header__title">
		@if($subtitle)
		<span class="section-header__sub"> 	{{ $subtitle }}</span>
		@endif
	{{ $title  }}
	</h2>
 </header>
